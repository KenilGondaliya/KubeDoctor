apiVersion: v1
kind: Pod

metadata:
  name: kubedoctor-pending-test
  namespace: default

spec:
  nodeSelector:
    kubedoctor-test: "does-not-exist"

  containers:
    - name: pending-container
      image: busybox:1.36
      command:
        - sh
        - -c
        - sleep 3600